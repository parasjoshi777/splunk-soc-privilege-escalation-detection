## Detection Overview

- `sudo_burst_detection.spl`
  Detects rapid sudo attempts within short time windows to identify possible
  privilege escalation or brute-force behavior.

- `severity_scoring.spl`
  Enhances detection by assigning severity levels based on frequency,
  helping SOC analysts prioritize incidents and reduce false positives.

